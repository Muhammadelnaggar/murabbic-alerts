// firebase-config.js (نسخة وهمية للنشر على GitHub)

console.warn("⚠️ firebase-config.js هو ملف وهمي ولا يحتوي على إعدادات Firebase حقيقية.");

// تصدير قيم فارغة لتجنب كسر الكود
export const db = null;
export const auth = null;
